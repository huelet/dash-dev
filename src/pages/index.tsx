import * as React from "react";
import { css, jsx } from "@emotion/react";

const IndexPage = () => {
  return (
    <div
      css={css`
        display: flex;
      `}
    >
    </div>
  );
};

export default IndexPage;
